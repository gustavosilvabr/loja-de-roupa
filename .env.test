# Os testes rodam sem nuvem: vazio faz `supabase` virar null e o app usar
# só o armazenamento local. Sem isso, cada teste faria chamadas de rede
# reais e a carga assíncrona sobrescreveria o estado no meio da execução.
VITE_SUPABASE_URL=
VITE_SUPABASE_KEY=
